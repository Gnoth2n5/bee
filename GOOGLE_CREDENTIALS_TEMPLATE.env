# ===========================================
# GOOGLE OAUTH CREDENTIALS TEMPLATE
# ===========================================
# Copy các dòng này vào file .env của bạn
# Thay thế your_client_id và your_client_secret bằng thông tin thực từ Google Console

# Google OAuth Configuration
GOOGLE_CLIENT_ID=your_client_id_here
GOOGLE_CLIENT_SECRET=your_client_secret_here

# App URL (đảm bảo không có dấu / ở cuối)
APP_URL=http://127.0.0.1:8000

# ===========================================
# HƯỚNG DẪN LẤY CREDENTIALS
# ===========================================

# Bước 1: Truy cập Google Cloud Console
# https://console.cloud.google.com/

# Bước 2: Tạo hoặc chọn project
# - Tạo project mới hoặc chọn project hiện có

# Bước 3: Kích hoạt Google+ API
# - Vào "APIs & Services" > "Library"
# - Tìm "Google+ API" hoặc "Google Identity"
# - Click "Enable"

# Bước 4: Tạo OAuth 2.0 Client ID
# - Vào "APIs & Services" > "Credentials"
# - Click "Create Credentials" > "OAuth 2.0 Client IDs"
# - Chọn "Web application"
# - Đặt tên: "BeeFood Web Client"

# Bước 5: Cấu hình Authorized Redirect URIs
# Thêm chính xác URL này:
# http://127.0.0.1:8000/auth/google/callback

# Bước 6: Copy thông tin
# - Copy Client ID và Client Secret
# - Thay thế vào file .env

# Bước 7: Clear cache và test
# php artisan config:clear
# php artisan cache:clear
# php artisan google:check-config 